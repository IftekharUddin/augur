[package]
name = "augur-capture"
version.workspace = true
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Window-scoped, read-only frame capture for Augur."
publish = false

[dependencies]
augur-core = { path = "../augur-core", version = "0.8.4" }
thiserror = "2.0"
